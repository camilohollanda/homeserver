apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Just the connector Deployment. The werify-production app owns the namespace and the `secrets` /
# ghcr-credentials secrets this pod consumes; this app only adds the connector on top.
resources:
  - connector-deployment.yaml
