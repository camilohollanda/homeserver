apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: werify-data
  namespace: werify-production
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-static
  volumeName: werify-production-data
  resources:
    requests:
      storage: 100Gi
