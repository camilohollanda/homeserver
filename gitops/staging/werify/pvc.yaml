apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: werify-data
  namespace: werify-staging
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-static
  volumeName: werify-staging-data
  resources:
    requests:
      storage: 100Gi
