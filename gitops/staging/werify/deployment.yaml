apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: werify-staging
  name: werify
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: werify
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: werify
    spec:
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: werify
          image: ghcr.io/prem-prakash/werify:staging
          imagePullPolicy: Always
          ports:
            - containerPort: 4000
              name: http
          startupProbe:
            httpGet:
              path: /health
              port: 4000
            periodSeconds: 2
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 4000
            periodSeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 4000
            periodSeconds: 10
            timeoutSeconds: 3
          env:
            - name: DNS_CLUSTER_QUERY
              value: cluster.werify-staging.svc.cluster.local
          envFrom:
            - secretRef:
                name: secrets
          volumeMounts:
            - name: data
              mountPath: /app/priv
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              # Typst PDF/A rendering peaks at ~1.5 GiB working-set on large
              # conversation docs; cgroup OOM killer hit at 2 GiB. 4 GiB gives
              # 2× headroom for transient spikes and concurrent renders.
              memory: "4Gi"
              # Typst PDF/A rendering pegged the previous 500m limit. 2 cores
              # lets layout + image processing run unthrottled.
              cpu: "2"
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: werify-data
---
apiVersion: v1
kind: Service
metadata:
  name: werify
  namespace: werify-staging
spec:
  type: ClusterIP
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app: werify
---
apiVersion: v1
kind: Service
metadata:
  name: cluster
  namespace: werify-staging
spec:
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: true
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app: werify
