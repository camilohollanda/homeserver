apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: miora-secrets
  namespace: miora-staging
spec:
  refreshInterval: 30s
  secretStoreRef:
    name: miora-staging
    kind: ClusterSecretStore
  target:
    name: secrets
    creationPolicy: Owner
  dataFrom:
    - find:
        name:
          regexp: ".*"
