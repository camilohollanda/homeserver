apiVersion: v1
kind: PersistentVolume
metadata:
  name: ntfy-cache
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-path-static
  local:
    path: /mnt/k8s-persistent/pvs/ntfy-cache
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - k3s-apps  # Update with actual node name from: kubectl get nodes
