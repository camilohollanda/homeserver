apiVersion: apps/v1
kind: Deployment
metadata:
  name: ntfy-bridge
  namespace: bugsink
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ntfy-bridge
  template:
    metadata:
      labels:
        app: ntfy-bridge
    spec:
      containers:
        - name: bridge
          image: python:3.13-alpine
          command: ["python", "/scripts/bridge.py"]
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: APPRISE_URL
              value: http://apprise.apprise.svc.cluster.local
            - name: APPRISE_KEY
              value: homeserver
            - name: TAG_FALLBACK
              value: bugsink
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
          volumeMounts:
            - name: script
              mountPath: /scripts
              readOnly: true
          resources:
            requests:
              memory: "32Mi"
              cpu: "10m"
            limits:
              memory: "128Mi"
              cpu: "100m"
      volumes:
        - name: script
          configMap:
            name: ntfy-bridge-script
            defaultMode: 0755
---
apiVersion: v1
kind: Service
metadata:
  name: ntfy-bridge
  namespace: bugsink
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: ntfy-bridge
