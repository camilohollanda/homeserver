#cloud-config
# Minimal OS prep only.
# Full PostgreSQL installation is handled by bootstrap/postgres/setup.sh (runs locally).

hostname: pg
manage_etc_hosts: true

package_update: true
package_upgrade: false

packages:
  - curl
  - ca-certificates
  - gnupg
