#cloud-config
# Minimal OS prep only.
# Full Infisical installation is handled by bootstrap/infisical/setup.sh (runs locally).

hostname: infisical
manage_etc_hosts: true

package_update: true
package_upgrade: false

packages:
  - curl
  - ca-certificates
  - gnupg
