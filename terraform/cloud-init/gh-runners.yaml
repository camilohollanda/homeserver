#cloud-config
# Minimal OS prep only.
# Full runner installation is handled by bootstrap/gh-runners/setup.sh (runs locally).

hostname: gh-runners
manage_etc_hosts: true

package_update: true
package_upgrade: false

packages:
  - curl
  - ca-certificates
  - gnupg
