#cloud-config
# Minimal OS prep only.
# Full media stack installation is handled by bootstrap/media/setup.sh (runs locally).

hostname: media-server
manage_etc_hosts: true

package_update: true
package_upgrade: false

packages:
  - curl
  - ca-certificates
  - gnupg
